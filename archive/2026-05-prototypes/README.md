# 2026-05 — Phase 1–4 prototype HTML

Loose HTML files that lived at the repo root during Phases 1–4. Snapshots of in-app surfaces or design exploration.

## Files

- **`one-more-cup.html`** — the magazine-style homepage prototype that became the basis for the unified app's homepage. Now superseded by `src/app/page.tsx`.
- **`coffee_passport_world.html`** — the Coffee Passport / world-map exploration. Concept absorbed into `src/components/CoffeeBeanWorldMap.tsx`.
- **`design-review.html`** — the interactive design review tool used during Phase 4. Replaced by the `reviews/` workflow.

## Why archived

Per `docs/WORKFLOW.md`, loose HTML at the repo root violates Phase 5 file structure. These files are kept here for reference; the active versions are in `src/`.
